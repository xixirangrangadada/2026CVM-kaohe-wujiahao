# 题2 AI 协作对话记录（占位）

> 本目录用于存放**题2（容器化持续 CPU Profiling 工具）**的 AI 编程完整对话记录。

## 当前状态：暂缓，尚未开展

题2 为**选做加分题**。当前进度：题1（①②③）已完成并 push 至 GitHub，本人正集中学习、消化题1 的原理与数据。

按计划，题2 将在题1 吃透后单独开展（预计一天、约 6 小时模式），届时与 AI 的**完整协作对话**（需求拆解 → 迭代 → 问题排查）会从本地会话导出至本目录，与题1③ 的对话记录同规格。

## 为什么现在没有对话

**未伪造。** 题2 尚未实际编码，故无真实 AI 协作记录。本占位如实说明，待题2 完成后补充真实对话导出。

## 题2 计划范围（据考题）

| 模块 | 要点 |
|---|---|
| 持续采集 | `perf record` 后台常驻，按时间窗口（如 1 min）轮转保存 |
| 历史保留 | 按时间戳命名，保留最近 N 小时（默认 24h），自动清理过期数据 |
| 按时间回查 | CLI/API 输入时间段，自动定位对应采样文件 |
| 一键火焰图 | 对指定时间段自动跑 FlameGraph 生成 SVG |
| 容器化 | Docker 镜像 + `.tar`，`--privileged`（PMU 访问），`docker load` + `docker run` 一键启动 |
| 测试验证 | stress-ng 构造 CPU 飙升场景，回查 + 火焰图验证热点 |

---

> 相关：题1 的 AI 协作真实对话记录见 `task1/3-cache-line-test/ai-chat-log/`。
