# Docker 构建上下文忽略清单（减小 build context、加速构建）
# 作用域：task2/src/ 为 build context（`docker build task2/src`）

# Python 缓存
__pycache__/
*.pyc
*.pyo

# 编辑器/系统
.DS_Store
*.swp
*~

# Docker 镜像导出（不进镜像）
*.tar
*.tar.gz

# 采样/日志数据（运行期产物，非镜像内容）
*.data
*.log
*.csv

# gitkeep 占位（入库用，镜像不需要）
.gitkeep
