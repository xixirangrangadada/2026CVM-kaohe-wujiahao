# perf 采样数据文件（大，不入库；原始 .txt 输出入库）
*.data
perf.data*
perf_*.data

# 编译产物
*.o
*.out
a.out
cache_line_test
stress-ng

# Docker 镜像导出（>100MB 放 GitHub Release，不入库）
*.tar

# 编辑器 / 系统
.DS_Store
*.swp
*~
__pycache__/
*.pyc

# 本地 PDF 生成工具（非考核产物，严格按考核目录标准不入库；
# 已生成的 report.pdf 已嵌入图表，不依赖本目录）
tools/
