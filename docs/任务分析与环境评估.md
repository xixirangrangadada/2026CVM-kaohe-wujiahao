# 2026 CVM 考核任务 — 上下文与分析记录

> 对 `CVM竞品微架构深度分析考题.md` 的任务拆解、环境评估、技术风险与执行路线。
> **ARM 云服务器版（鲲鹏 920 / TaiShan v110）**。
> 更新：2026-06-14。

---

## 一、任务概览

| 题 | 性质 | 内容 |
|---|---|---|
| 题1 | 必做 | ① 五场景 perf stat 微架构指标 ② 火焰图 ③ AI 辅助 Cache Line 微基准 |
| 题2 | 选做加分 | 容器化 7×24 持续 CPU Profiling（**暂缓**，题1 完成后视时间）|

**交付**：GitHub 公开仓库 `xixirangrangadada/2026CVM-kaohe-wujiahao`，目录结构考题规定死。

---

## 二、环境评估（ARM 云，已实测）

### 2.1 硬件（主机 kp-022）

- **CPU**：鲲鹏 920（HiSilicon，implementer 0x48 / part 0xd01），**TaiShan v110** 微架构，aarch64，4 核 @2.6GHz
- **缓存**：L1d/i 256KiB / L2 2MiB / **L3 128MiB**
- **cache line**：L1/L2 = 64B，**L3 = 128B**（题1③ 要标双拐点）

> 鲲鹏 920 是华为服务器 ARM 芯片、TaiShan v110 核、大 L3——本身就是好的微架构分析素材。

### 2.2 虚拟化与权限

- `systemd-detect-virt = kvm`（KVM guest，非裸金属，但 **PMU 透传 OK**）
- `perf_event_paranoid = -1`（**全开**，root，无权限阻塞）

### 2.3 工具

| 工具 | 状态 | 备注 |
|---|---|---|
| perf 4.19.90 | ✅ 自带 | PMU 事件用 raw code 引用（见 2.4）|
| numactl / cpupower / gcc / make / perl / python3 | ✅ | |
| **stress-ng 0.21.03** | ✅ **源码编译** | openEuler 仓库无；`git clone` + `make`。5 个 method（int64/matrixprod/queens/read64/rand-set）全可用 |
| git 2.23.0 | ✅ | `dnf install git` |
| docker | ⬜ | 题2 再装 |
| FlameGraph | ⬜ | 题1② `git clone` |

### 2.4 PMU 事件（关键，已实测）

**通用名直接可用**：cycles, instructions, cache-references, cache-misses, L1-dcache/icache-load-misses, branch-misses, dTLB/iTLB-load-misses, context-switches, cpu-migrations

**2 个 perf 4.19 不认符号名，用 raw event code 引用**：

| 题目事件 | ARM 原生事件 | code | 写法 |
|---|---|---|---|
| LLC-load-misses | ll_cache_miss_rd | 0x037 | `armv8_pmuv3_0/event=0x037,name=LLC-load-misses/` |
| branch-instructions | br_retired | 0x021 | `armv8_pmuv3_0/event=0x021,name=branch-instructions/` |

> perf 4.19 解析 `pmu/符号名/` 时把符号名当成 raw config term → 报 "unknown term"；必须用 `pmu/event=0xNN/` 或 `rNN` 短形式。事件 code 从 `/sys/bus/event_source/devices/armv8_pmuv3_0/events/<name>` 读。

---

## 三、技术风险（已全部解决）

- ~~虚拟化下 PMU `<not supported>`~~ → KVM 透传 OK，4 关键事件 3 个直接有真实数字、1 个用 raw code 替代，全部解决
- ~~perf 4.19 不认 PMU 符号名~~ → raw event code 写法破解
- ~~stress-ng openEuler 仓库无~~ → 源码编译

---

## 四、决策与路线

**环境定为 ARM 云（kp-022），通过 `ssh arm`（`~/.ssh/config` 别名，ed25519 密钥免密）协作。阶段0 已完成。**

### 环境选型复盘（WSL2 → ARM）

1. 初选本地 WSL2（i7-12700K / Alder Lake 8P+4E，异构本身是分析素材）。
2. 阶段0 实测：WSL2 的 Hyper-V 层**不向 guest 透传 PMU MSR**，且 `microsoft-standard-WSL2` 内核**不带 Alder Lake 的 PMU 驱动**。
3. `dmesg` 一锤定音：`unsupported p6 CPU model 151 no PMU driver, software events only`（model 151 = 0x97 = Alder Lake）。`perf list hw` 为空，cycles/instructions 全 `<not supported>`，只剩 software events。
4. 结论：换裸金属/原生 Linux。候选 ARM 云（鲲鹏 920）。
5. 实测 ARM 云：KVM guest 但 `perf list` 有 `armv8_pmuv3` 事件、`paranoid=-1`、4 关键事件可用 → **定为执行环境**。

> **方法论沉淀**：盯死 4 个关键事件（cache-misses / L1-dcache-load-misses / LLC-load-misses / dTLB-load-misses）作判据，一次跑完看结果；沿"环境栈"（shell → perf → perf_event → 内核 → 虚拟化 → 硬件）逐层下钻定位断裂层；最终答案在 `dmesg` / `/sys` / `/proc`，不在 perf 的报错文案里。

---

## 五、协作模式

- **本地 Windows**（git 仓库 `E:\wujiahao\2026CVM-kaohe-wujiahao`）：代码/文档本地维护（学习 + 版本可控），commit + push。
- **云端 ARM**（`ssh arm`）：perf 采集、编译、火焰图生成。复杂脚本走 heredoc：`ssh arm 'bash -s' <<'REMOTE'`（避免引号/路径转义）。
- 代码本地写 → `scp` 上云端跑 → 产物（perf 输出 .txt、SVG 火焰图）回传本地入库。
- 工作约定：**只读探查可直接跑；代码/装包命令先 review 再执行。** 代码运行输出都贴出来看。

---

## 六、各小题技术要点（ARM 视角）

### 题1① perf stat 五场景
- 5 负载（stress-ng 0.21.03）：int64→ALU / matrixprod→FPU+SIMD / read64→内存带宽+L3 / rand-set→TLB+CacheMiss / queens→分支预测
- 事件集：通用名 + 2 raw code（见 2.4）
- **每条命令加 `taskset -c 0~3`** 钉死一个核（鲲鹏 920 同构 4 核，避免线程迁移噪声；无 P/E 异构）
- 衍生指标：IPC=ins/cyc、L1/LLC/TLB Miss Rate、分支预测失败率

### 题1② 火焰图
- `perf record -F 99 -g` → `perf script` → `stackcollapse-perf.pl` → `flamegraph.pl`
- 计算密集 = "尖塔"（热点集中）；访存/分支 = "扁平"（热点分散）
- 内核态函数（`__do_page_fault` / `copy_page` / TLB refill）出现要解释成因

### 题1③ Cache Line 微基准 ⚠️ 最易踩坑
- **pointer chasing**（链表节点、节点间距 = stride、沿指针链遍历，绕过硬件预取器）；朴素 `for i: sum += a[i*stride]` 会被预取器抹平拐点
- stride 1/2/4/8/16/32/64/128/256，每 stride 测延迟 + `perf stat` 采 L1/LLC miss（LLC 用 `ll_cache_miss_rd`）
- stride=1 vs stride=64 出火焰图
- **拐点：64B（L1/L2 line）+ 128B（L3 line，鲲鹏 920 特色）**，曲线标双拐点 + 微架构解释

### 题2 容器化 Profiling（暂缓）
- 采集守护 `perf record -F 99 -ag` / 60s 轮转 / 24h 保留自动清理 / 时间段回查 / 一键火焰图
- Dockerfile（aarch64）+ `--privileged` 运行
- 评审为腾讯导师，ARM 镜像声明架构即可

---

## 七、待办

- [x] 阶段0：ssh arm / PMU 方案 / stress-ng / 仓库骨架 + push
- [ ] 文档 ARM 化（本批，进行中）
- [ ] 题1① 五场景采集
- [ ] 题1② 火焰图
- [ ] 题1③ Cache Line 微基准
- [ ] 题2（视时间）
- [ ] 收尾（README / 简历 / ai-chat-log / push）
